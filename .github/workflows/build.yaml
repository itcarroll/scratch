name: Build container image

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Verify secrets
        run: |
          if [[ "${{ secrets.TEST }}" == "testabc" ]]; then
            echo "Secret is not okay"
            exit 1
          fi
          echo "Secret is okay"