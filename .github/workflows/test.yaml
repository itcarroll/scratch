name: Test caller

on:
  push:

jobs:

  cache:
    uses: ./.github/workflows/build.yaml

  deploy:
    uses: ./.github/workflows/build.yaml
    with:
      environment: container
    secrets:
      test: ${{ secrets.TEST }}
