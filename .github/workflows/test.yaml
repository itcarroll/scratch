name: Test caller

on:
  push:

jobs:
  call:
    uses: ./.github/workflows/build.yaml
    with:
      environment: container
    secrets:
      test: ${{ secrets.TEST }}
    # with:
    #   environment: container
